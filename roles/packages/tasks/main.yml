---

# Install packages via apt
- name: Install core system and packaging prerequisites
  apt:
    name:
      - apt-transport-https
      - ca-certificates
      - gnupg
      - lsb-release
      - software-properties-common
    state: latest
    update_cache: yes
  ignore_errors: yes
- name: Install base command-line utilities
  apt:
    name:
      - curl
      - wget
      - git
      - vim
      - mc
      - htop
      - fastfetch
    state: latest
  ignore_errors: yes
- name: Install Python and automation tooling
  apt:
    name:
      - python3
      - python3-pip
      - python3-venv
    state: latest
  ignore_errors: yes
- name: Install desktop and GNOME support packages
  apt:
    name:
      - dbus-x11
      - dconf-cli
      - snapd
    state: latest
  ignore_errors: yes
- name: Install networking and security tools
  apt:
    name:
      - ftp
      - net-tools
      - inetutils-traceroute
      - tshark
      - tcptraceroute
      - tcpdump
      - telnet
      - sshpass
      - stunnel4
    state: latest
  ignore_errors: yes
- name: Install virtualization integration tools
  apt:
    name:
      - linux-virtual
      - linux-cloud-tools-virtual
      - linux-tools-virtual
      - qemu-utils
      - libguestfs-tools
    state: latest
  ignore_errors: yes
- name: Install system services and utilities
  apt:
    name:
      - openssh-server
    state: latest
  ignore_errors: yes

- name: Update ubuntu packages [please wait]
  apt:
    name: "*"
    state: latest
    cache_valid_time: 3600
    update_cache: yes
  ignore_errors: yes

# Install packages via snap
- name: Install bruno snap package
  become: yes
  snap:
    name: bruno
    state: present
