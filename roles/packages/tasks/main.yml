---

# Install packages via apt
- name: Install core system and packaging prerequisites
  apt:
    name:
      - apt-transport-https
      - ca-certificates
      - gnupg
      - lsb-release
      - software-properties-common
    state: latest
    update_cache: yes
  ignore_errors: no

- name: Set package names based on Ubuntu major version
  set_fact:
    visual_pkg: "{{ 'fastfetch' if (ansible_distribution_major_version | default(0) | int) >= 26 else 'neofetch' }}"
    net_pkg: "{{ 'tshark' if (ansible_distribution_major_version | default(0) | int) >= 26 else 'tcptrace' }}"

- name: Install base command-line utilities
  apt:
    name:
      - curl
      - wget
      - git
      - vim
      - mc
      - htop
      - "{{ visual_pkg }}"
    state: latest
  ignore_errors: no
- name: Install Python and automation tooling
  apt:
    name:
      - python3
      - python3-pip
      - python3-venv
    state: latest
  ignore_errors: no
- name: Install desktop and GNOME support packages
  apt:
    name:
      - dbus-x11
      - dconf-cli
      - snapd
    state: latest
  ignore_errors: no
- name: Install networking and security tools
  apt:
    name:
      - ftp
      - net-tools
      - inetutils-traceroute
      - "{{ net_pkg }}"
      - tcptraceroute
      - tcpdump
      - telnet
      - sshpass
      - stunnel4
    state: latest
  ignore_errors: no
- name: Install virtualization integration tools
  apt:
    name:
      - linux-virtual
      - linux-cloud-tools-virtual
      - linux-tools-virtual
      - qemu-utils
      - libguestfs-tools
    state: latest
  ignore_errors: no
- name: Install system services and utilities
  apt:
    name:
      - openssh-server
    state: latest
  ignore_errors: no

- name: Update ubuntu packages [please wait]
  apt:
    name: "*"
    state: latest
    cache_valid_time: 3600
    update_cache: yes
  ignore_errors: no

# Install packages via snap
- name: Install bruno snap package
  become: yes
  snap:
    name: bruno
    state: present
